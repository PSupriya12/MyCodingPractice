#include <iostream>
using namespace std;

int main() {
	// your code goes here
	
	int t;
	cin >> t;
	while(t--)
	{
	    int n, l, x;
	    cin >> n >> l >> x;
	    if(( n - l) < l)
	        cout << ((n - l) * x) << endl;
	        
	    else
	        cout << (l * x) << endl;
	}
	return 0;
}
