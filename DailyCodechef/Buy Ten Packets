#include <iostream>
using namespace std;

int main() {
	// your code goes here
	
	int t;
	cin >> t;
	while(t--)
	{
	    int x, y;
	    cin >> x >> y;
	    if(y == (2 * x))
	        cout << (5 * x) << endl;
	        
	    else 
	        cout << ((y *2) + (x)) << endl;
	}
	return 0;
}
