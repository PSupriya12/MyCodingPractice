#include <iostream>
using namespace std;

int main() {
	// your code goes here
	
	int t;
	cin >> t;
	while(t--)
	{
	    int x, y;
	    cin >> x >> y;
	    int cnt;
	    if(y < (x / 2))
	        cnt = y;
	        
	    else
	        cnt = x / 2;
	        
	    cout << cnt << endl;
	}
	return 0;
}
