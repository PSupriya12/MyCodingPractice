#include <iostream>
using namespace std;

int main() {
	// your code goes here
	
	int t;
	cin >> t;
	while(t--)
	{
	    int a, b, c;
	    cin >> a >> b >> c;
	    int k;
	    for(k = 1; k < 100; k++)
	    {
	        if(a % k != 0 && b % k != 0 && c % k != 0)
	            break;
	    }
	    cout << k << endl;
	}
	return 0;
}
